// Reserved for future multi-step auth flows (email verification, MFA).
// The current sign-in / register form lives directly in AuthGateView
// because its state is trivial and binding to @EnvironmentObject is cleaner.

// The inbox screen keeps its state directly in InboxView using @State, since
// the container's services are reachable via @EnvironmentObject. This file
// is kept as a placeholder for the day inbox logic grows past that pattern
// (e.g. background refresh, optimistic mutations across multiple screens).

// Profile screen state is kept in ProfileView via @State + the environment's
// services, mirroring InboxView. This placeholder exists for future
// cross-screen profile orchestration (e.g. listening for ProfileUpdated
// events from a push channel and reflecting them everywhere).
